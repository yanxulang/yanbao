引「标准:日期」为 日期；
引「标准:测试」为 测试；
引「标准:文字」为 文字；
引「../src/审计.yx」为 审计；

法 发现（代码：文，严重度：文）：典 则
    归 {「code」：代码，「severity」：严重度，「package」：「示例包」，「message」：「示例发现」}；
终

法 应失败（动作：法（）：数，代码：文）则
    令 已失败 为 假；
    试 则
        动作（）；
    救 错误 则
        置 已失败 为 真；
        测试.断言（文字.始于（错误.消息，「[」加 代码 加「]」），「审计错误须报告稳定诊断代码」）；
    终
    测试.断言（已失败，「无效审计配置或数据必须失败」）；
终

测试.断言（日期.是否合法（审计.当前日期（）），「审计时钟须返回合法 UTC 日历日期」）；

测试.相等（审计.拒绝级别（【】），「high」）；
测试.相等（审计.拒绝级别（【「--deny-warnings」】），「medium」）；
测试.相等（审计.拒绝级别（【「--deny-level」，「严重」】），「critical」）；
测试.相等（审计.拒绝级别（【「--deny-level」，「高」】），「high」）；
测试.相等（审计.拒绝级别（【「--deny-level」，「warning」】），「medium」）；
测试.相等（审计.拒绝级别（【「--拒绝级别」，「低」】），「low」）；
测试.相等（审计.拒绝级别（【「--deny-level」，「信息」】），「info」）；
测试.相等（审计.拒绝级别（【「--deny-level」，「none」】），「none」）；

测试.相等（审计.规范严重度（「critical」），「critical」）；
测试.相等（审计.规范严重度（「error」），「high」）；
测试.相等（审计.规范严重度（「warning」），「medium」）；
测试.相等（审计.规范严重度（「low」），「low」）；
测试.相等（审计.规范严重度（「information」），「info」）；

定 五级发现 为【
    发现（「AUDIT_CRITICAL」，「critical」），发现（「AUDIT_HIGH」，「high」），
    发现（「AUDIT_MEDIUM」，「warning」），发现（「AUDIT_LOW」，「low」），
    发现（「AUDIT_INFO」，「info」）
】；
定 默认评估 为 审计.评估（五级发现，【】，「2026-07-18」）；
测试.相等（默认评估【「deny_level」】，「high」）；
测试.相等（默认评估【「denied_count」】，2）；
测试.相等（默认评估【「severity_counts」】【「critical」】，1）；
测试.相等（默认评估【「severity_counts」】【「high」】，1）；
测试.相等（默认评估【「severity_counts」】【「medium」】，1）；
测试.相等（默认评估【「severity_counts」】【「low」】，1）；
测试.相等（默认评估【「severity_counts」】【「info」】，1）；
测试.相等（审计.评估（五级发现，【「--deny-level」，「critical」】，「2026-07-18」）【「denied_count」】，1）；
测试.相等（审计.评估（五级发现，【「--deny-level」，「medium」】，「2026-07-18」）【「denied_count」】，3）；
测试.相等（审计.评估（五级发现，【「--deny-level」，「low」】，「2026-07-18」）【「denied_count」】，4）；
测试.相等（审计.评估（五级发现，【「--deny-level」，「info」】，「2026-07-18」）【「denied_count」】，5）；
测试.相等（审计.评估（五级发现，【「--deny-level」，「none」】，「2026-07-18」）【「denied_count」】，0）；

定 漏洞代码 为「AUDIT_VULNERABILITY_YXSA_2026_0001」；
定 漏洞发现 为 发现（漏洞代码，「high」）；
测试.相等（审计.评估（【漏洞发现】，【】，「2026-07-18」）【「denied_count」】，1）；
定 精确漏洞抑制 为 审计.评估（【漏洞发现】，【「--suppress」，漏洞代码 加「@2026-08-01@等待安全版本」】，「2026-07-18」）；
测试.相等（精确漏洞抑制【「denied_count」】，0）；
测试.相等（精确漏洞抑制【「suppressed_count」】，1）；
定 错误漏洞抑制 为 审计.评估（【漏洞发现】，【「--suppress」，「AUDIT_VULNERABILITY_OTHER@2026-08-01@另一公告」】，「2026-07-18」）；
测试.相等（错误漏洞抑制【「denied_count」】，1）；

测试.断言（文字.始于（审计.修复建议（漏洞代码），「升级」），「漏洞建议须要求升级或移除」）；
测试.断言（文字.始于（审计.修复建议（「AUDIT_VULNERABILITY_METADATA_MISSING」），「要求索引」），「缺失元数据须由索引发布者补齐」）；
测试.断言（文字.始于（审计.修复建议（「AUDIT_GIT_SYMBOLIC_REVISION」），「把 Git」），「Git 建议须要求精确修订」）；
测试.断言（文字.始于（审计.修复建议（「AUDIT_NATIVE_UNSIGNED」），「核对原生」），「原生建议须覆盖签名与目标」）；

定 有效抑制 为 审计.评估（五级发现，【「--suppress」，「AUDIT_HIGH@2026-07-19@等待上游修复」】，「2026-07-18」）；
测试.相等（有效抑制【「denied_count」】，1）；
测试.相等（有效抑制【「suppressed_count」】，1）；
测试.断言（有效抑制【「items」】【1】【「suppressed」】，「有效抑制应精确匹配诊断代码」）；
测试.相等（有效抑制【「items」】【1】【「suppression」】【「reason」】，「等待上游修复」）；

定 当日抑制 为 审计.解析抑制（「AUDIT_HIGH@2026-07-18@当日复核」，「2026-07-18」）；
测试.断言（当日抑制【「active」】，「到期当天抑制仍应有效」）；
定 过期抑制 为 审计.评估（【发现（「AUDIT_HIGH」，「high」）】，【「--deny-level」，「medium」，「--suppress」，「AUDIT_HIGH@2026-07-17@旧例外」】，「2026-07-18」）；
测试.相等（过期抑制【「suppressed_count」】，0）；
测试.相等（长度（过期抑制【「expired_suppressions」】），1）；
测试.相等（过期抑制【「denied_count」】，2）；

法 无效级别（）：数 则 审计.拒绝级别（【「--deny-level」，「fatal」】）；归 0；终
法 冲突级别（）：数 则 审计.拒绝级别（【「--deny-warnings」，「--deny-level」，「high」】）；归 0；终
法 缺少原因（）：数 则 审计.解析抑制（「AUDIT_HIGH@2026-08-01@」，「2026-07-18」）；归 0；终
法 非法日期（）：数 则 审计.解析抑制（「AUDIT_HIGH@2026-02-30@复核」，「2026-07-18」）；归 0；终
法 重复抑制（）：数 则 审计.抑制列（【「--suppress」，「AUDIT_HIGH@2026-08-01@甲」，「--抑制」，「AUDIT_HIGH@2026-08-02@乙」】，「2026-07-18」）；归 0；终
法 未知严重度（）：数 则 审计.评估（【发现（「AUDIT_UNKNOWN」，「fatal」）】，【】，「2026-07-18」）；归 0；终
法 非对象发现（）：数 则 审计.评估（【1】，【】，「2026-07-18」）；归 0；终
法 缺代码发现（）：数 则 审计.评估（【{「severity」：「high」，「package」：「示例」，「message」：「缺代码」}】，【】，「2026-07-18」）；归 0；终
法 非文字代码（）：数 则 审计.评估（【{「code」：1，「severity」：「high」，「package」：「示例」，「message」：「坏代码」}】，【】，「2026-07-18」）；归 0；终
法 含分隔代码（）：数 则 审计.评估（【发现（「AUDIT@BAD」，「high」）】，【】，「2026-07-18」）；归 0；终
应失败（无效级别，「AUDIT_CONFIG」）；
应失败（冲突级别，「AUDIT_CONFIG」）；
应失败（缺少原因，「AUDIT_CONFIG」）；
应失败（非法日期，「AUDIT_CONFIG」）；
应失败（重复抑制，「AUDIT_CONFIG」）；
应失败（未知严重度，「AUDIT_SCHEMA」）；
应失败（非对象发现，「AUDIT_SCHEMA」）；
应失败（缺代码发现，「AUDIT_SCHEMA」）；
应失败（非文字代码，「AUDIT_SCHEMA」）；
应失败（含分隔代码，「AUDIT_SCHEMA」）；
