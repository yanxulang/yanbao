引「标准:测试」为 测试；
引「标准:文字」为 文字；
引「../src/输出.yx」为 输出；

测试.相等（输出.配置（「build」，【「--message-format」，「json」】），「json」）；
输出.设置命令（「build」）；输出.设置阶段（「compile」）；输出.设置项目根（「/tmp/project」）；
输出.消息（「开始构建」）；
输出.变更（「cache_update」，「/tmp/project/.cache」，「」，{「hit」：假}）；
输出.制品（「yxb」，「/tmp/project/build/app.yxb」，「abc」，「」，{}）；
输出.诊断（「WARN001」，「warning」，「示例警告」，「src/主.yx」，空，「检查配置」，空）；
定 结果 为 输出.结果（真，0）；
测试.相等（结果【「schema_version」】，1）；
测试.相等（结果【「type」】，「result」）；
测试.相等（结果【「command」】，「build」）；
测试.相等（结果【「success」】，真）；
测试.相等（结果【「exit_code」】，0）；
测试.相等（结果【「project_root」】，「/tmp/project」）；
测试.相等（结果【「phase」】，「complete」）；
测试.相等（长度（结果【「diagnostics」】），1）；
测试.相等（长度（结果【「changes」】），1）；
测试.相等（长度（结果【「artifacts」】），1）；
测试.相等（长度（结果【「messages」】），1）；
测试.断言（结果【「duration_ms」】不小于 0，「耗时不得为负」）；

测试.相等（输出.配置（「version」，【「--消息格式」，「json-lines」】），「json-lines」）；
测试.相等（输出.格式（），「json-lines」）；

法 应拒绝格式（格式：文，代码：文）则
    令 已失败 为 假；
    试 则 输出.配置（「version」，【「--message-format」，格式】）；
    救 错误 则 置 已失败 为 真；测试.断言（文字.始于（错误.消息，「[」加 代码 加「]」），「格式错误代码须稳定」）；终
    测试.断言（已失败，「无效格式必须失败」）；
终
应拒绝格式（「xml」，「CLI010」）；
应拒绝格式（「」，「CLI010」）；

令 缺值失败 为 假；
试 则 输出.配置（「version」，【「--message-format」，「--help」】）；
救 错误 则 置 缺值失败 为 真；测试.断言（文字.始于（错误.消息，「[CLI003]」），「格式缺值须使用 CLI003」）；终
测试.断言（缺值失败，「格式缺值必须失败」）；
