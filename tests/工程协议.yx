引「标准:测试」为 测试；
引「标准:文字」为 文字；
引「../src/核心.yx」为 核心；
引「../src/输出.yx」为 输出；

定 审计必需检查 为【
    「lock_checksum_sha256」，「source_transport」，「git_exact_revision」，「spdx_license」，
    「registry_yanked」，「registry_vulnerabilities」，「duplicate_versions」，「target_match」，
    「native_abi」，「native_target」，「native_checksum」，「native_provenance」
】；

法 审计能力（检查：列<文>，版本）：典 则
    归 {「schema_version」：版本，「checks」：检查}；
终

法 审计握手（能力：典）：典 则
    归 {
        「yanxu_version」：「1.1.18」，
        「operation_capabilities」：{「audit」：能力}
    }；
终

法 应拒绝审计能力（动作：法（）：数）则
    令 已失败 为 假；
    试 则
        动作（）；
    救 错误 则
        置 已失败 为 真；
        测试.断言（文字.始于（错误.消息，「[AUDIT_CAPABILITY_MISSING]」），「审计能力错误须使用稳定代码」）；
    终
    测试.断言（已失败，「缺失、不完整或不一致的审计能力必须失败」）；
终

法 旧核心缺少能力（）：数 则
    核心.确认审计握手（{「yanxu_version」：「1.1.17」}）；归 0；
终

法 握手检查不完整（）：数 则
    核心.确认审计握手（审计握手（审计能力（审计必需检查【：11】，1）））；归 0；
终

法 握手版本无效（）：数 则
    核心.确认审计握手（审计握手（审计能力（审计必需检查，2）））；归 0；
终

法 响应缺少能力（）：数 则
    定 握手能力 为 核心.确认审计握手（审计握手（审计能力（审计必需检查，1）））；
    核心.确认审计响应（握手能力，{「findings」：【】}）；归 0；
终

法 响应能力不一致（）：数 则
    定 握手能力 为 核心.确认审计握手（审计握手（审计能力（审计必需检查，1）））；
    核心.确认审计响应（握手能力，{
        「audit_capabilities」：审计能力（审计必需检查【1：】，1），「findings」：【】
    }）；归 0；
终

法 响应版本无效（）：数 则
    定 握手能力 为 核心.确认审计握手（审计握手（审计能力（审计必需检查，1）））；
    核心.确认审计响应（握手能力，{
        「audit_capabilities」：审计能力（审计必需检查，2），「findings」：【】
    }）；归 0；
终

应拒绝审计能力（旧核心缺少能力）；
应拒绝审计能力（握手检查不完整）；
应拒绝审计能力（握手版本无效）；
应拒绝审计能力（响应缺少能力）；
应拒绝审计能力（响应能力不一致）；
应拒绝审计能力（响应版本无效）；

定 完整握手能力 为 核心.确认审计握手（审计握手（审计能力（审计必需检查，1）））；
测试.相等（完整握手能力【「schema_version」】，1）；
测试.相等（长度（完整握手能力【「checks」】），长度（审计必需检查））；
逐 下标 于 范围（0，长度（审计必需检查））则
    测试.相等（完整握手能力【「checks」】【下标】，审计必需检查【下标】）；
终
定 扩展检查 为【
    「lock_checksum_sha256」，「source_transport」，「git_exact_revision」，「spdx_license」，
    「registry_yanked」，「registry_vulnerabilities」，「duplicate_versions」，「target_match」，
    「native_abi」，「native_target」，「native_checksum」，「native_provenance」，
    「future_transparency_log」
】；
定 扩展握手 为 审计握手（审计能力（扩展检查，1））；
置 扩展握手【「operation_capabilities」】【「future_operation」】为 {「schema_version」：7}；
定 扩展握手能力 为 核心.确认审计握手（扩展握手）；
测试.相等（长度（扩展握手能力【「checks」】），12）；
定 扩展响应检查 为【
    「future_signature_format」，「native_provenance」，「native_checksum」，「native_target」，
    「native_abi」，「target_match」，「duplicate_versions」，「registry_vulnerabilities」，
    「registry_yanked」，「spdx_license」，「git_exact_revision」，「source_transport」，
    「lock_checksum_sha256」
】；
定 扩展响应能力 为 核心.确认审计响应（扩展握手能力，{
    「audit_capabilities」：审计能力（扩展响应检查，1），
    「findings」：【】，「future_field」：真
}）；
测试.相等（长度（扩展响应能力【「checks」】），12）；
测试.相等（扩展响应能力【「checks」】【0】，「lock_checksum_sha256」）；
测试.相等（扩展响应能力【「checks」】【11】，「native_provenance」）；

定 成功外壳 为 核心.分析工程响应（{
    「protocol_version」：1，「ok」：真，「result」：{「value」：42}，
    「future_field」：{「ignored」：真}
}）；
测试.相等（成功外壳【「kind」】，「success」）；
测试.相等（成功外壳【「result」】【「value」】，42）；
测试.相等（核心.分析工程正文（「  」）【「error」】【「code」】，「YANXU_PROTOCOL_EMPTY」）；
测试.相等（核心.分析工程正文（「not-json」）【「error」】【「code」】，「YANXU_PROTOCOL_JSON_INVALID」）；
测试.相等（核心.分析工程正文（「 {\"protocol_version\":1,\"ok\":true,\"result\":{\"value\":42}} 」）【「result」】【「value」】，42）；

定 旧错误外壳 为 核心.分析工程响应（{
    「protocol_version」：1，「ok」：假，
    「error」：{「code」：「ENGINEERING_ERROR」，「message」：「旧核心错误」}
}）；
测试.相等（旧错误外壳【「kind」】，「engineering_error」）；
测试.相等（旧错误外壳【「error」】【「code」】，「ENGINEERING_ERROR」）；
测试.相等（旧错误外壳【「error」】【「file」】，「」）；
测试.相等（旧错误外壳【「error」】【「range」】，空）；

定 带位置错误外壳 为 核心.分析工程响应（{
    「protocol_version」：1，「ok」：假，
    「error」：{
        「code」：「ENGINEERING_MANIFEST_INVALID」，「message」：「清单格式无效」，
        「operation」：「inspect」，「path」：「/tmp/项目/言序.toml」，「line」：9，
        「future_field」：「ignored」
    }
}）；
测试.相等（带位置错误外壳【「error」】【「file」】，「/tmp/项目/言序.toml」）；
测试.相等（带位置错误外壳【「error」】【「range」】【「start」】【「line」】，9）；
测试.相等（带位置错误外壳【「error」】【「range」】【「end」】【「column」】，1）；

测试.相等（核心.分析工程响应（【】）【「error」】【「code」】，「YANXU_PROTOCOL_SCHEMA_INVALID」）；
测试.相等（核心.分析工程响应（{「protocol_version」：2，「ok」：真，「result」：{}}）【「error」】【「code」】，「YANXU_PROTOCOL_VERSION_UNSUPPORTED」）；
测试.相等（核心.分析工程响应（{「protocol_version」：「1」，「ok」：真，「result」：{}}）【「error」】【「code」】，「YANXU_PROTOCOL_SCHEMA_INVALID」）；
测试.相等（核心.分析工程响应（{「protocol_version」：1，「ok」：「true」，「result」：{}}）【「error」】【「code」】，「YANXU_PROTOCOL_SCHEMA_INVALID」）；
测试.相等（核心.分析工程响应（{「protocol_version」：1，「ok」：真}）【「error」】【「code」】，「YANXU_PROTOCOL_SCHEMA_INVALID」）；
测试.相等（核心.分析工程响应（{「protocol_version」：1，「ok」：假，「error」：{「code」：「BAD」，「message」：「坏代码」}}）【「error」】【「code」】，「YANXU_PROTOCOL_SCHEMA_INVALID」）；
测试.相等（核心.分析工程响应（{「protocol_version」：1，「ok」：假，「error」：{「code」：「ENGINEERING_IO」，「message」：「错误」，「line」：1}}）【「error」】【「code」】，「YANXU_PROTOCOL_SCHEMA_INVALID」）；

定 握手 为 核心.工程请求（{「operation」：「handshake」}）；
测试.断言（长度（握手【「yanxu_version」】）大于 0，「工程握手应报告言序版本」）；
测试.相等（握手【「manifest_formats」】【1】，2）；
测试.相等（握手【「lock_formats」】【1】，2）；
测试.相等（握手【「yxb_formats」】【0】，1）；
测试.相等（握手【「native_abi」】【0】，1）；
测试.相等（握手【「native_abi」】【1】，2）；
测试.断言（核心.列含（握手【「operations」】，「bundle」），「工程协议应支持 Bundle」）；
测试.断言（非 核心.工具链版本兼容（「1.1.16」），「不得接受缺少长超时能力的旧工具链」）；
测试.断言（核心.工具链版本兼容（「1.1.17」），「应接受最低支持版本」）；
测试.断言（核心.工具链版本兼容（「1.2.0」），「应接受更新的 1.x 版本」）；
测试.断言（核心.工具链版本兼容（「2.0.0」），「应接受更高主版本的工具链握手」）；
测试.断言（非 核心.工具链版本兼容（「1.1.17-rc.1」），「最低核心版本的预发布不得伪装成稳定基线」）；
测试.断言（核心.工具链版本兼容（「1.1.18-dev.1」），「应允许更高核心版本的开发联调」）；
测试.断言（核心.工具链版本兼容（「1.1.17+build.1」），「构建元数据不应改变稳定版本兼容性」）；
测试.断言（核心.工具链版本兼容（「1.1.17+build-1」），「构建元数据中的连字符不得被当作预发布标记」）；
测试.断言（非 核心.工具链版本兼容（「无效版本」），「无效版本必须被拒绝」）；
测试.相等（核心.确认工具链（）【「yanxu_version」】，握手【「yanxu_version」】）；
测试.断言（核心.有参数（【「--offline」，「--dev」】，「--dev」），「应识别开关」）；
测试.相等（核心.文字选项（【「--name」，「工程」】，「--name」，「默认」），「工程」）；
测试.断言（非 核心.有参数（【「--」，「--offline」】，「--offline」），「不得读取透传旗标」）；
测试.相等（核心.文字选项（【「--」，「--name」，「透传值」】，「--name」，「默认」），「默认」）；

输出.配置（「protocol-test」，【「--message-format」，「json」】）；
令 未知操作失败 为 假；
试 则
    核心.工程请求（{「operation」：「unsupported-operation」}）；
救 错误 则
    置 未知操作失败 为 真；
    测试.断言（文字.始于（错误.消息，「[ENGINEERING_」），「有效核心错误须保留稳定代码」）；
终
测试.断言（未知操作失败，「未知工程操作必须失败」）；
定 协议失败结果 为 输出.结果（假，1）；
测试.相等（长度（协议失败结果【「diagnostics」】），1）；
测试.断言（文字.始于（协议失败结果【「diagnostics」】【0】【「code」】，「ENGINEERING_」），「核心错误代码须进入结构化诊断」）；
测试.相等（协议失败结果【「diagnostics」】【0】【「file」】，「」）；
测试.相等（协议失败结果【「diagnostics」】【0】【「range」】，空）；
测试.相等（协议失败结果【「diagnostics」】【0】【「original_exit_code」】，1）；

定 短包 为 核心.GitHub依赖（「json」）；
测试.相等（短包【「alias」】，「json」）；
测试.相等（短包【「package」】，「yanxu-json」）；
测试.相等（短包【「url」】，「https://github.com/yanxulang/yanxu-json.git」）；
测试.相等（核心.GitHub依赖（「yanxu-json」）【「url」】，短包【「url」】）；
定 第三方 为 核心.GitHub依赖（「acme/yanxu-tools」）；
测试.相等（第三方【「alias」】，「tools」）；
测试.相等（第三方【「package」】，「yanxu-tools」）；
测试.相等（第三方【「url」】，「https://github.com/acme/yanxu-tools.git」）；

定 注册表言窗 为 核心.言窗依赖请求（「暂存项目」，「」）；
测试.相等（注册表言窗【「operation」】，「edit」）；
测试.相等（注册表言窗【「path」】，「暂存项目」）；
测试.相等（注册表言窗【「alias」】，「言窗」）；
测试.相等（注册表言窗【「package」】，「yanxu-gui」）；
测试.相等（注册表言窗【「version」】，「^1.0」）；
测试.相等（注册表言窗【「source」】，「registry」）；
测试.相等（注册表言窗【「source_value」】，「https://get.yanxu.dev/packages/v1」）；
测试.断言（非 注册表言窗【「development」】，「言窗应写入普通依赖」）；

定 本地言窗 为 核心.言窗依赖请求（「暂存项目」，「../yanxu-gui」）；
测试.相等（本地言窗【「source」】，「path」）；
测试.相等（本地言窗【「source_value」】，「../yanxu-gui」）；
测试.相等（本地言窗【「version」】，「^1.0」）；
