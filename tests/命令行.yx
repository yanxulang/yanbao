引「标准:测试」为 测试；
引「标准:文字」为 文字；
引「../src/命令行.yx」为 命令行；

法 应失败（命令：文，参数：列<文>，代码：文）则
    令 已失败 为 假；
    试 则
        命令行.解析（命令，参数）；
    救 错误 则
        置 已失败 为 真；
        测试.断言（文字.始于（错误.消息，「[」加 代码 加「]」），「参数错误须报告稳定诊断代码」）；
    终
    测试.断言（已失败，「无效参数必须失败」）；
终

测试.相等（命令行.规范命令（「新」），「new」）；
测试.相等（命令行.规范命令（「构」），「build」）；
测试.相等（命令行.规范命令（「审」），「audit」）；

定 添加 为 命令行.解析（「add」，【「--version」，「1.2.3」，「示例包」，「--dev」，「--dev」】）；
测试.相等（添加【「command」】，「add」）；
测试.相等（添加【「arguments」】【0】，「示例包」）；
测试.相等（添加【「arguments」】【1】，「--version」）；
测试.相等（添加【「arguments」】【2】，「1.2.3」）；
测试.相等（添加【「arguments」】【3】，「--dev」）；
测试.相等（添加【「arguments」】【4】，「--dev」）；

定 透传 为 命令行.解析（「行」，【「--manifest-path」，「项目」，「--」，「--timeout」，「原样值」】）；
测试.相等（透传【「command」】，「run」）；
测试.相等（透传【「passthrough」】【0】，「--timeout」）；
测试.相等（透传【「passthrough」】【1】，「原样值」）；
测试.相等（透传【「arguments」】【2】，「--」）；

测试.断言（命令行.解析（「build」，【「--help」】）【「help」】，「子命令应支持帮助旗标」）；
测试.断言（长度（命令行.帮助文字（「build」））大于 0，「子命令帮助不能为空」）；
测试.相等（命令行.解析（「新」，【「项目」，「--图形」】）【「arguments」】【1】，「--图形」）；
测试.相等（命令行.解析（「version」，【「--timeout」，「5000」】）【「arguments」】【1】，「5000」）；
测试.相等（命令行.解析（「help」，【「build」，「--message-format」，「json」】）【「arguments」】【2】，「json」）；
测试.断言（命令行.解析（「add」，【「包」，「--recover」】）【「arguments」】【1】等于「--recover」，「依赖编辑应支持恢复旗标」）；

定 审计抑制 为 命令行.解析（「audit」，【
    「--suppress」，「AUDIT_ONE@2026-08-01@等待上游修复」，
    「--抑制」，「AUDIT_TWO@2026-08-02@已完成风险复核」，
    「--json」
】）；
测试.相等（审计抑制【「arguments」】【1】，「AUDIT_ONE@2026-08-01@等待上游修复」）；
测试.相等（审计抑制【「arguments」】【3】，「AUDIT_TWO@2026-08-02@已完成风险复核」）；
测试.相等（审计抑制【「arguments」】【4】，「--json」）；

逐 命令 于【「new」，「install」，「update」，「check」，「test」，「run」，「build」，「bundle」，「clean」，「tree」，「doctor」，「doc」，「outdated」，「pack」，「vendor」，「audit」，「version」，「help」】则
    测试.相等（命令行.解析（命令，【「--message-format」，「json」】）【「arguments」】【1】，「json」）；
终
逐 命令 于【「add」，「remove」，「why」】则
    测试.相等（命令行.解析（命令，【「位置参数」，「--message-format」，「json-lines」】）【「arguments」】【2】，「json-lines」）；
终

应失败（「clean」，【「--manifest-path」】，「CLI003」）；
应失败（「add」，【「包名」，「--version」】，「CLI003」）；
应失败（「build」，【「--unknown-option」】，「CLI002」）；
应失败（「build」，【「--standalone」，「--bundle」】，「CLI004」）；
应失败（「bundle」，【「--standalone」】，「CLI002」）；
应失败（「add」，【「包名」，「--git」，「地址」，「--path」，「路径」】，「CLI004」）；
应失败（「add」，【「--offline」】，「CLI005」）；
应失败（「clean」，【「多余位置」】，「CLI006」）；
应失败（「clean」，【「--manifest-path」，「甲」，「--manifest-path」，「乙」】，「CLI007」）；
应失败（「clean」，【「--」，「--once」】，「CLI008」）；
应失败（「check」，【「--timeout」，「1000」，「--超时」，「2000」】，「CLI004」）；
应失败（「check」，【「--message-format」，「json」，「--消息格式」，「human」】，「CLI004」）；
应失败（「new」，【「--recover」，「--恢复」】，「CLI004」）；
应失败（「audit」，【「--suppress」】，「CLI003」）；
应失败（「audit」，【「--deny-level」，「high」，「--拒绝级别」，「medium」】，「CLI004」）；
应失败（「audit」，【「--deny-warnings」，「--deny-level」，「medium」】，「CLI004」）；
应失败（「audit」，【「--json」，「--message-format」，「json」】，「CLI004」）；
应失败（「不存在的命令」，【】，「CLI001」）；
