引「标准:测试」为 测试；
引「标准:文件」为 文件；
引「标准:路径」为 路径；
引「../src/事务.yx」为 事务；

定 根 为 路径.合并（「build」，「yanbao-transaction-test」）；
若 文件.存在（根）则 文件.删除（根，真）；终
文件.创建目录（根）；
定 锁 为 路径.合并（路径.合并（根，「nested」），「lock」）；
测试.断言（事务.取得独占（锁），「首次应取得事务锁」）；
测试.断言（非 事务.取得独占（锁），「第二次不得取得同一事务锁」）；

定 来源 为 路径.合并（根，「source.txt」）；
定 目标 为 路径.合并（根，「target.txt」）；
文件.写入（来源，「完整内容」）；
事务.移动（来源，目标，假）；
测试.断言（非 文件.存在（来源），「移动后来源应消失」）；
测试.相等（文件.读取（目标），「完整内容」）；

事务.原子写文字（目标，「替换内容」）；
测试.相等（文件.读取（目标），「替换内容」）；
事务.还原文字文件（目标，真，「完整内容」）；
测试.相等（文件.读取（目标），「完整内容」）；
事务.还原文字文件（目标，假，「」）；
测试.断言（非 文件.存在（目标），「原先不存在的文件应在恢复时删除」）；

定 布局 为 {「lock」：锁，「journal」：路径.合并（锁，「journal.json」）}；
事务.写日志（布局，{「schema_version」：1，「phase」：「prepared」}）；
测试.相等（事务.读日志（布局）【「phase」】，「prepared」）；
事务.写日志（布局，{「schema_version」：1，「phase」：「committed」}）；
测试.相等（事务.读日志（布局）【「phase」】，「committed」）；

事务.释放（布局）；
测试.断言（非 文件.存在（锁），「释放后事务锁应消失」）；
文件.删除（根，真）；
